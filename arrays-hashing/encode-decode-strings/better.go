package main

type BetterSolution struct {
}

func (bs *BetterSolution) Encode(strs []string) string {
	if len(strs) <= 0 {
		return ""
	}

	res := ""
	for i, v := range strs {
		j := i
		for ; j <len(strs); j++ {
			if strs[i]
		}
	}

	return ""
}

func (bs *BetterSolution) Decode(str string) []string {

	return []string{}
}
